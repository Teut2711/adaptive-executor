[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --cov=adaptive_executor --cov-report=term-missing --cov-report=html
markers =
    integration: slow integration tests
    unit: fast isolated tests
